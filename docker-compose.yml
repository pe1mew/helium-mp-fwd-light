version: '2'

services:

  mpfwd:
    build: ./mp-fwd
    privileged: true
    volumes:
      - 'pktfwdr:/var/pktfwd'

  gatewayrs:
    build: ./hm-gatewayrs
    expose:
      - "1680"   
    # ports:
    #   - "1680:1680/udp"
    volumes:
      - 'miner-storage:/var/data'
      - 'miner-log:/var/log/miner'
      - 'pktfwdr:/var/pktfwd'

volumes:
  miner-storage:
  miner-log:
  pktfwdr:

